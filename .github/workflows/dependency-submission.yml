name: Dependency graph

on:
  push:
    branches: [master]

permissions:
  contents: write

jobs:
  dependency-submission:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: Set up JDK 25
        uses: actions/setup-java@3a4f6e1af504cf6a31855fa899c6aa5355ba6c12 # v4.7.0
        with:
          java-version: '25'
          distribution: 'temurin'

      # This action requires the repository's Dependency graph to be enabled
      # in Settings -> Code security and analysis. If it is disabled, the step
      # will fail with a clear message, so we continue-on-error to keep CI green.
      - name: Generate and submit dependency graph
        uses: gradle/actions/dependency-submission@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0
        continue-on-error: true
